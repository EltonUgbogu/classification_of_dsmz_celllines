name: python-graph-env
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.10
  - pandas
  - numpy
  - matplotlib
  - seaborn
  - networkx
  - scipy
  - scikit-learn
  - jupyter
  - ipython
  - pip
  # Note: graph-tool is not available via pip, removed from dependencies
